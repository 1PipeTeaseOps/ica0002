- name: Install nginx
  ansible.builtin.apt:
      name: nginx
      state: present

- name: Template Nginx Config
  ansible.builtin.template:
      src: default.j2
      dest: /etc/nginx/sites-enabled/default
      mode: '0644'
  notify: 
    - restart nginx
